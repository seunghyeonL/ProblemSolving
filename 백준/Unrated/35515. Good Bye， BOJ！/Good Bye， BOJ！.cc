#include <bits/stdc++.h>
using namespace std;

int main(int argc, char const *argv[])
{
    // #include <bits/stdc++.h>
    // using namespace std;

    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    // ifstream inputFileStream("input.txt");

    string plz_come_back = R"(
        알고리즘에 재미붙이고 열심히 푼지 이제 반년쯤 밖에 안 됐는데 아쉬워요. (최근 한 두달은 잘 안풀긴 했어요..)
        좋은 사이트 제공 감사했습니다. 다른 형태로 BOJ가 돌아오길 기다리고 있을게요!! 
    )";

    cout << plz_come_back << '\n';

    // inputFileStream.close();
    return 0;
}